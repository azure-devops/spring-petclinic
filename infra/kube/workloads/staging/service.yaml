kind: Service
apiVersion: v1
metadata:
  name: pet-clinic
  namespace: staging
  labels:
    version: pc-dfbc328  
spec:
  type: LoadBalancer
  selector:
    app: pet-clinic
    version: pc-dfbc328 
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
